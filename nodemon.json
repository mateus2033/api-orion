{
  "verbose": true,
  "watch": ["src"],
  "ext": "ts,js,json",
  "ignore": ["src/**/*.spec.ts", "src/**/*.test.ts"],
  "legacyWatch": true,
  "polling": true,
  "pollInterval": 1000,
  "exec": "ts-node src/server.ts",
  "env": {
    "NODE_ENV": "development"
  }
}